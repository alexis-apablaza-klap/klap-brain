node_modules/
*.log
.env
.env.*

# Token de GitHub Packages -- nunca commitear (ver knowledge/release-publish/)
.npmrc

# Estado local, no del repo
.klap/

# Salida de klap graph build / debug local
*.tmp.json
