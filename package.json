{
  "name": "@alexis-apablaza-klap/klap-brain",
  "version": "0.1.0",
  "description": "Ecosistema de contexto compartido KLAP SVA: topologia derivada, memoria funcional y skills.",
  "bin": {
    "klap": "bin/klap.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "ISC",
  "author": "Equipo KLAP SVA",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "budget": "node src/ci/budget.js",
    "validate": "node src/ci/validate.js",
    "no-duplicate:check": "node src/ci/no-duplicate-check.js",
    "versions:check": "node src/ci/versions-check.js",
    "scan:check": "node src/ci/scan-check.js",
    "ci": "npm run budget && npm run validate && npm run no-duplicate:check && npm run versions:check && npm run scan:check"
  },
  "dependencies": {},
  "keywords": [
    "klap",
    "klap-brain",
    "topology",
    "claude",
    "mcp",
    "cli"
  ]
}
